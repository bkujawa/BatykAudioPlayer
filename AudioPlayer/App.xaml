<Application x:Class="BatykAudioPlayer.APP.AudioPlayer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BatykAudioPlayer.APP"
             StartupUri="AudioPlayerView.xaml"
             >
    <Application.Resources>
        <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Background" Value="Black" />
            <Setter Property="Foreground" Value="White" />
        </Style>

        <!--  Work with this style to show time of sound on right edge of grid  -->
        <Style x:Key="SoundListStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="Black" />
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate DataType="{x:Type ListBox}">
                        <Grid 

                              ToolTip="{Binding Path}"
                              >
                            <StackPanel Orientation="Horizontal">

                                <TextBlock 
                                           FontSize="16"
                                           Foreground="DarkSlateGray"
                                           Margin="0,0,8,0"
                                           Text="{Binding Time}"
                                           />
                                <TextBlock 
                                           FontSize="20"
                                           Foreground="White"
                                           Text="{Binding Name}"
                                           />
                            </StackPanel>

                        </Grid>
                        <!-- <DockPanel HorizontalAlignment="Stretch" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type VirtualizingStackPanel}, Mode=FindAncestor}}">
                            <TextBlock DockPanel.Dock="Left"
                                       FontSize="20"
                                       Text="{Binding Name}"/>
                            <TextBlock DockPanel.Dock="Right"
                                       FontSize="20"
                                       Text="{Binding Time}"/>
                        </DockPanel>-->
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="SelectedItem" Value="True">
                    <Setter Property="Background" Value="Gray" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MenuButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="20" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="FontWeight" Value="Light" />
            <Setter Property="FontSize" Value="11" />
        </Style>

        <Style x:Key="RoundedButtonStyle"
               BasedOn="{StaticResource ButtonStyle}"
               TargetType="{x:Type Button}"
               >
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border>
                            <Border.Style>
                                <Style TargetType="{x:Type Border}">
                                    <Setter Property="CornerRadius" Value="15" />
                                    <Setter Property="BorderThickness" Value="2" />
                                    <Setter Property="Padding" Value="2" />
                                    <Setter Property="BorderBrush" Value="White" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="Red" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Background" Value="Red" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseCaptureWithin" Value="True">
                    <Setter Property="BorderBrush" Value="Black" />
                </Trigger>

            </Style.Triggers>
        </Style>

    </Application.Resources>
</Application>
